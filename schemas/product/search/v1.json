{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "description": "Schema for discovering the product search endpoint (dev.ocp.product.search@1.0).",
  "properties": {
    "urlTemplate": {
      "type": "string",
      "description": "An RFC 6570 URL Template for the search endpoint. The client should substitute the required variables.",
      "example": "https://api.example.com/products/search?q={query}&sort={sortBy}"
    },
    "supportedSorts": {
      "type": "array",
      "items": { "type": "string" },
      "description": "A list of valid values for the {sortBy} variable.",
      "example": ["relevance", "price_asc", "price_desc"]
    },
    "searchableIdentifierTypes": {
      "type": "array",
      "description": "A list of identifier types from dev.ocp.product.identifiers@1.0 that are indexed and can be used in search queries.",
      "items": {
        "type": "string"
      },
      "example": ["SKU", "UPC", "MPN"]
    }
  },
  "required": ["urlTemplate"]
}
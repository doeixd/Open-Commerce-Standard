{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "description": "Schema for the dev.ocp.order.direct@1.0 capability. Its presence indicates the server supports creating orders directly from a list of items, bypassing the cart entirely. This enables streamlined 'Buy Now' flows for simple, one-click purchases.",
  "properties": {
    "maxItemsPerOrder": {
      "type": "integer",
      "minimum": 1,
      "description": "The maximum number of items allowed in a single direct order. If omitted, there is no server-imposed limit."
    }
  }
}
